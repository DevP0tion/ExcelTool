{
  "name": "xlmcp",
  "description": "Excel automation MCP server (49 tools). Session pooling, virtual clipboard, chunked parallel I/O.",
  "version": "0.4.0",
  "userConfig": {
    "pool_size": {
      "title": "Pool Size",
      "description": "PowerShell session pool size (1-8)",
      "type": "string",
      "required": false,
      "default": "4"
    }
  }
}
